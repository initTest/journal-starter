# name: Build and Push to ECR

# on:
#   push:
#     branches: [ main ]

# env:
#   ECR_REGISTRY: ${{ secrets.ECR_REGISTRY }}      # or set as repo secret
#   ECR_REPOSITORY: ${{ secrets.ECR_REPOSITORY }}  # or set as repo secret
#   IMAGE_TAG: ${{ github.sha }}

# jobs:
#   build:
#     runs-on: ubuntu-latest
#     # if your secrets live in a GitHub Environment (see screenshot),
#     # uncomment the next line and replace <your-environment-name>
#     # environment: <your-environment-name>

#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4

#       - name: Configure AWS credentials
#         uses: aws-actions/configure-aws-credentials@v2
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: ${{ secrets.AWS_REGION }}

#       - name: Login to ECR
#         uses: aws-actions/amazon-ecr-login@v1

#       - name: Set up QEMU
#         uses: docker/setup-qemu-action@v2

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v2

#       - name: Build and push image to ECR
#         uses: docker/build-push-action@v4
#         with:
#           context: .
#           file: ./Dockerfile
#           push: true
#           tags: |
#             ${{ env.ECR_REGISTRY }}/${{ env.ECR_REPOSITORY }}:latest
#             ${{ env.ECR_REGISTRY }}/${{ env.ECR_REPOSITORY }}:${{ env.IMAGE_TAG }}
#           cache-from: type=gha
#           cache-to: type=gha,mode=max